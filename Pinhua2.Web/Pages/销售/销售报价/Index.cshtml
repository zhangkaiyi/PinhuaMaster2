@page
@model Pinhua2.Web.Pages.销售.销售报价.IndexModel
@using Pinhua2.Common.Attributes

@{
    ViewData["Title"] = "Index";
}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center mb-3 mt-3">
    <h3>销售报价</h3>
    <div class="btn-group">
        <a class="btn btn-outline-secondary" asp-page="Create">新增</a>
    </div>
</div>
<div class="table-responsive">
    <table id="indexMain" class="table table-bordered text-nowrap"
           data-toggle="table">
        <thead>
            <tr>
                @foreach (var cdm in CustomDisplayFactory<vm_销售报价>.CustomDisplayModels(Model.vm_销售报价表.FirstOrDefault()))
                {
                    if (cdm.ForIndex)
                    {
                        <th>
                            @cdm.Name
                        </th>
                    }
                }
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.vm_销售报价表)
            {
                <tr>
                    @foreach (var cdm in CustomDisplayFactory<vm_销售报价>.CustomDisplayModels(item))
                    {
                        if (cdm.ForIndex)
                        {
                            <td>
                                @{
                                    if (cdm.IsDatetime)
                                    {
                                        @(((DateTime?)cdm.Value)?.ToString("yyyy-MM-dd"))
                                    }
                                    else
                                    {
                                        @cdm.Value
                                    }
                                }
                            </td>
                        }
                    }
                    <td>
                        <a asp-page="./Edit" asp-route-id="@item.RecordId">修改</a> |
                        <a asp-page="./Details" asp-route-id="@item.RecordId">查看</a> |
                        <a asp-page="./Delete" asp-route-id="@item.RecordId">删除</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<div id="detailsGroup">
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center mb-3 mt-3">
        <h3>明细</h3>
    </div>
    <div class="table-responsive">
        <table id="indexDetails" class="table table-bordered text-nowrap"
               data-toggle="table">
            <thead>
                <tr>
                    @foreach (var cdm in CustomDisplayFactory<vm_销售报价D>.CustomDisplayModels(default(vm_销售报价D)))
                    {
                        if (cdm.ForIndex)
                        {
                            <th data-field="@(cdm.RawName)">@(cdm.Name)</th>
                        }
                    }
                </tr>
            </thead>
        </table>
    </div>
</div>

@section Scripts{
    <script>
        $(function () {
            $('#indexDetails').on('load-success.bs.table', function (e, data) {
                //$('#detailsGroup').css('display', 'block')
            })
            $('#indexMain').on('click-row.bs.table', function (e, row, $element, field) {
                var url = ["/api/sd/报价/", row[0]].join("")
                console.log("vue:", "customerIdOnChange, url:", url)
                $('#indexDetails').bootstrapTable('refresh', { url: url, silent: true });
            })
        })
    </script>
}