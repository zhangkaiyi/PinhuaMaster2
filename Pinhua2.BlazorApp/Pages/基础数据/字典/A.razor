@inherits ABase
@page "/基础数据/字典"

<KLayoutColContent2>
    <ColContent1>
        <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center mb-3">
            <h3>字典</h3>
            <div>
                <button type="button" class="btn btn-primary" @onclick="@(e=>Navigation.NavigateTo(routeC))"><i class="oi oi-pencil"></i> 新增</button>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <KTable2 @ref="mainsTable" DataSource="mainsTableDataSource" IsResponsive="true" IsBordered=" true" IsHovarable="true" IsTextNoWrap="true"
                         OnRowClicked="@(OnRowClicked)" PageSize="10" CheckBoxHeader="true" IsClickToSelect="true" IsSingleSelect="true">
                    <KTable2Columns>
                        <KTable2Column Width="1" Text="操作">
                            @{
                                var row = context as dto字典;
                                <a href="javascript:;" @onclick="@(e => Navigation.NavigateTo($"{routeR}/{row?.RecordId}"))" class="mr-1">查看</a>
                                <a href="javascript:;" @onclick="@(e => Navigation.NavigateTo($"{routeU}/{row?.RecordId}"))" class="mr-1">修改</a>
                                <a href="javascript:;" @onclick="@(e => Navigation.NavigateTo($"{routeD}/{row?.RecordId}"))">删除</a>
                            }
                        </KTable2Column>
                        <KTable2Column Width="500" Field="@(nameof(dto字典.字典号))"></KTable2Column>
                        <KTable2Column Width="500" Field="@(nameof(dto字典.字典名))"></KTable2Column>
                        <KTable2Column Width="500" Field="@(nameof(dto字典.组号))"></KTable2Column>
                        <KTable2Column Width="500" Field="@(nameof(dto字典.组名))"></KTable2Column>
                    </KTable2Columns>
                </KTable2>
            </div>
        </div>
    </ColContent1>
    <ColContent2>
        <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center my-3 mt-md-0">
            <h3>明细</h3>
        </div>

        <div class="row">
            <div class="col-12">
                <KTable2 DataSource="detailsTableDataSource" IsResponsive="true" IsBordered=" true" IsTextNoWrap="true" @ref="detailsTable"
                         CheckBoxHeader="true" IsClickToSelect="true" IsSingleSelect="true">
                    <KTable2Columns>
                        <KTable2Column Width="500" Field="@(nameof(dto字典D.编号))"></KTable2Column>
                        <KTable2Column Width="500" Field="@(nameof(dto字典D.名称))"></KTable2Column>
                        <KTable2Column Width="500" Field="@(nameof(dto字典D.代码))"></KTable2Column>
                    </KTable2Columns>
                </KTable2>
            </div>
        </div>
    </ColContent2>
</KLayoutColContent2>